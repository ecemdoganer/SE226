#include <iostream>
using namespace std;

int sum(int n) {
    
    if (n == 0) 
        return 0;
        
    return (1 / n) + sum(n - 1);
}

int main (){
    int n;
    cout << "Enter step size: ";
    cin >> n;
    cout << sum(n) << endl;
    return 0;
    
}
